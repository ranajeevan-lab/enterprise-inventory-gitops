apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enterprise-inventory.fullname" . }}-test-connection"
  labels:
    {{- include "enterprise-inventory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enterprise-inventory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
